[project]
name = "trajectories"
version = "0.1.0"
requires-python = ">=3.11"

[tool.uv.workspace]
members = [
    "packages/trajectory-schema",
    "apps/viewer-api",
    "apps/trajgen",
]

[tool.uv.sources]
trajectory-schema = { workspace = true }
